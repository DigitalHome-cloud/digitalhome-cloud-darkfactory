# Amplify Gen 2 — local sandbox state, regenerated per developer
amplify_outputs.json
amplify_outputs.d.ts
.amplify/

# Each app's src/amplify_outputs.json IS committed (in the submodule repos)
# — those hold public Cognito + AppSync IDs. Only the umbrella's root copy
# is ignored, since `npx ampx sandbox` writes there per-developer.

# Local scratch space (deploy logs, screenshots, etc.) — never commit
todo/

# Dependencies
node_modules/

# Gatsby
.cache/
public/

# Environment & secrets
.env*
!.env.example

# OS
.DS_Store
Thumbs.db
*.swp
*~

# IDE
.idea/
.vscode/
*.code-workspace
